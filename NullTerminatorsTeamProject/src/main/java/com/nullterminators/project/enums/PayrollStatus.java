package com.nullterminators.project.enums;

/**
 * Enum representing the status of a payroll entry.
 */
public enum PayrollStatus {
    ALREADY_COMPLETED,
    ALREADY_EXISTS,
    EMPLOYEE_NOT_FOUND,
    ERROR,
    INVALID_DATA,
    INVALID_FORMAT,
    NOT_FOUND,
    OK,
    SUCCESS
}
